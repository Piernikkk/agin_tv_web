import { css } from "@/styled-system/css";

export const sourceComponentContainer = css({
    padding: '15px 20px',
    borderRadius: '15px',
    maxWidth: 'fit-content',
    display: 'flex',
    alignItems: 'center',
    gap: '20px',
    border: '1px solid',
    borderColor: 'border'
});

export const sourceComponentInline = css({
    display: 'flex',
    alignItems: 'center',
    gap: '10px',
})