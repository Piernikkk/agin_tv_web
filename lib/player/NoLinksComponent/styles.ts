import { css } from "@/styled-system/css";

export const noLinksComponentContainer = css({
    position: 'absolute',
    left: 0,
    right: 0,
    bottom: 0,
    top: 0,
    alignItems: 'center',
    justifyContent: 'center',
    display: 'flex',
    flexDirection: 'column',
    color: 'red',
    gap: 10,
})