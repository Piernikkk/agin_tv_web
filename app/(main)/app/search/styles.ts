import { css } from "@/styled-system/css";

export const searchPageContainer = css({
    display: 'flex',
    flexDirection: 'column',
    gap: '20px',
    padding: '50px',
    overflow: 'auto',
    height: '100%',
})

export const noInputContainer = css({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    height: '100%',
})